# RAW Script Backend (Páginas Infinitas)

Backend simples e funcional para gerar **páginas RAW infinitas** usando IDs numéricos (`0,1,2,3...`), compatível com **Roblox loadstring**.

## 🚀 Funcionalidades
- Geração automática de IDs (`/raw/0`, `/raw/1`, `/raw/2`…)
- Retorna **texto puro (text/plain)**
- Sem blob
- Sem GitHub Pages
- Funciona com `loadstring(game:HttpGet())`
- Banco simples em `scripts.json`

---

## 📁 Estrutura do Projeto